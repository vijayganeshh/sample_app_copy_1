<%= form_for @poll do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :topic, placeholder: "Ask a question..." %>
  </div>
  <%= f.submit "Create Poll", class: "btn btn-primary" %>
<% end %>